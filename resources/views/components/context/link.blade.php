<li>
    <a href="{{ $link }}"
        class="flex px-4 py-2 text-gray-600 no-underline transition-colors duration-100 hover:text-primary-500 hover:no-underline"
        @click.prevent="open=false">{{ $title }}</a>

</li>
